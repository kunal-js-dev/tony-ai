# ── Core FastAPI stack ────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
aiofiles>=23.2.1

# ── HTTP client (Ollama calls) ────────────────────────────────────────────────
httpx>=0.27.0

# ── Document processors ───────────────────────────────────────────────────────
pymupdf>=1.24.0          # PDF text extraction (fitz)
pdfplumber>=0.11.0       # PDF table extraction
pandas>=2.2.0            # Excel/CSV analysis
openpyxl>=3.1.0          # xlsx engine for pandas
python-pptx>=0.6.23      # PowerPoint extraction
python-docx>=1.1.0       # Word document extraction
pillow>=10.3.0           # Image processing
pytesseract>=0.3.10      # OCR engine wrapper

# ── Vector database (local) ──────────────────────────────────────────────────
chromadb>=0.5.0

# ── System utilities ──────────────────────────────────────────────────────────
psutil>=5.9.0

# ── Legacy Flask backend (app.py) — only needed if running old Flask server ──
flask>=3.0.0
flask-cors>=4.0.0
requests>=2.31.0
